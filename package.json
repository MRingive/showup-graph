{
  "name": "showup",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ mringive/the-show-up-club",
    "create-local": "graph create --node http://localhost:8020/ mringive/showup",
    "remove-local": "graph remove --node http://localhost:8020/ mringive/showup",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 mringive/showup"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
